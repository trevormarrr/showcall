# server
PORT=3200
MOCK=0

# Resolume target
RESOLUME_HOST=10.1.110.72
RESOLUME_PORT=8080

# ---- HTTP endpoint templates (adjust to your plugin) ----
# Trigger a single clip in a layer/column
# Use {layer} and {column} tokens – the server will substitute numbers.
RESOLUME_TRIGGER_TEMPLATE=/api/v1/composition/layers/{layer}/columns/{column}/trigger

# Trigger an entire column (all layers)
RESOLUME_TRIGGER_COLUMN_TEMPLATE=/api/v1/composition/columns/{column}/trigger

# Perform a preview->program cut/take
RESOLUME_CUT_PATH=/api/v1/transport/take

# Clear all (or program) – set to your plugin’s clear endpoint or leave blank to disable
RESOLUME_CLEAR_PATH=/api/v1/transport/clear

# Fetch status snapshot – should return program/preview info
RESOLUME_STATUS_PATH=/api/v1/status
