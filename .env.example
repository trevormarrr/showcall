# ShowCall Environment Configuration
# Copy this file to .env and update with your Resolume settings

# ShowCall server port (local)
PORT=3200

# Resolume computer IP address
# Find this in Resolume → Preferences → OSC or Web Server
RESOLUME_HOST=10.1.110.146

# Resolume REST API port (for monitoring)
# Default: 8080 (set in Resolume → Preferences → Web Server)
RESOLUME_REST_PORT=8080

# Resolume OSC port (for control)
# Default: 7000 (set in Resolume → Preferences → OSC → Input)
RESOLUME_OSC_PORT=7000

# Resolume events WebSocket port (for SMPTE/timecode events)
# Usually same as REST port; Default: 8080
RESOLUME_EVENTS_PORT=8080

# Mock mode (for testing without Resolume)
# Set to 1 to enable, 0 to disable
MOCK=0
